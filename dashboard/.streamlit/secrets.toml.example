# Copy to secrets.toml (gitignored) and fill after creating the Supabase project.
# Use the ANON (public) key here — NEVER the service_role key. RLS limits staff to
# read-only. Can also be supplied via SUPABASE_URL / SUPABASE_ANON_KEY env vars.
SUPABASE_URL = ""
SUPABASE_ANON_KEY = ""
